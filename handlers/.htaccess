# Prevent people from listing directory contents
Options -Indexes

# Block direct access to PHP files (optional stricter version)
<FilesMatch "\.php$">
  Order Deny,Allow
  Deny from all
  Allow from env=REDIRECT_STATUS
</FilesMatch>
